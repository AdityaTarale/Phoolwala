import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  section: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-around',
    paddingVertical: 10
  }
});
