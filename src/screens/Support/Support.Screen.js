import React from 'react';
import { View } from 'react-native';

const propTypes = {};
const defaultProps = {};

export const SupportScreen = () => {
  return <View />;
};

SupportScreen.propTypes = propTypes;
SupportScreen.defaultProps = defaultProps;
